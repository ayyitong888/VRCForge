{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Restrict baseline permissions to the main VRCForge window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
