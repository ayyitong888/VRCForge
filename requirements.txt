# VRCForge Python dependencies.
# Tested with Python 3.12+; Python 3.14 should also work as long as wheels are available.

# Dashboard server and API models.
fastapi>=0.115.0
pydantic>=2.7.0,<3.0.0
uvicorn>=0.30.0

# LLM providers.
anthropic>=0.34.0
google-genai>=1.16.1
openai>=1.30.0

# VRCForge-owned Unity MCP Core client helper. The server is bundled in the
# Unity package and must not be installed as a Python runtime dependency.

# Signed offline .vsk skill packages.
cryptography>=43.0.0

# Local validation and tests.
httpx>=0.27.0
pytest>=8.0.0
