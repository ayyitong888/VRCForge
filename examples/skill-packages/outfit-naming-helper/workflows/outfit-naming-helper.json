{
  "schema": "vrcforge.skill-package.workflow.v1",
  "mode": "read_only",
  "steps": [
    {
      "name": "scan_bindings_before_naming_proposal",
      "tool": "vrcforge_scan_animation_bindings",
      "writes": false,
      "request": {
        "proposalOnly": true,
        "includeObjectPaths": true,
        "includeParameterBindings": true
      }
    }
  ],
  "blockedTargets": {
    "object": "Blocked until VRCForge can atomically migrate animation, component, and constraint path references.",
    "parameter": "Blocked until VRCForge can atomically migrate expression-menu, FX-condition, and animation references."
  },
  "approval": {
    "required": false
  },
  "checkpoint": {
    "required": false
  },
  "rollback": {
    "required": false
  }
}
